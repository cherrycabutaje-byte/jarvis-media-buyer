import { writeFileSync } from "fs";
const content = [
`"use client";`,
`import { useState, useEffect } from "react";`,
`import ReactMarkdown from "react-markdown";`,
`import jsPDF from "jspdf";`,
`import Link from "next/link";`,
``,
`function splitVerdictAndReport(text) {`,
`  const verdictMatch = text.match(/(## JARVIS VERDICT[\\s\\S]*?---\\n)([\\s\\S]*)/);`,
`  if (verdictMatch) {`,
`    return { verdict: verdictMatch[1].replace(/---\\n$/, "").trim(), report: verdictMatch[2].trim() };`,
`  }`,
`  return { verdict: "", report: text };`,
`}`,
``,
`export default function AnalyzePage() {`,
`  const [input, setInput] = useState("");`,
`  const [url, setUrl] = useState("");`,
`  const [activeTab, setActiveTab] = useState("paste");`,
`  const [analysis, setAnalysis] = useState("");`,
`  const [loading, setLoading] = useState(false);`,
`  const [fetching, setFetching] = useState(false);`,
`  const [fetchMessage, setFetchMessage] = useState("");`,
`  const [error, setError] = useState("");`,
`  const [loadingStep, setLoadingStep] = useState(0);`,
`  const [email, setEmail] = useState("");`,
`  const [emailSubmitted, setEmailSubmitted] = useState(false);`,
`  const [emailLoading, setEmailLoading] = useState(false);`,
`  const [mode, setMode] = useState("full");`,
``,
`  const loadingSteps = [`,
`    "Running Consumer Psychologist Analysis...",`,
`    "Running Media Buyer Analysis...",`,
`    "Running Growth Strategist Analysis...",`,
`    "Running Offer Strategist Analysis...",`,
`    "JARVIS is synthesizing all expert findings...",`,
`  ];`,
``,
`  useEffect(() => {`,
`    if (!loading) { setLoadingStep(0); return; }`,
`    let step = 0;`,
`    setLoadingStep(0);`,
`    function advance() {`,
`      step += 1;`,
`      if (step < loadingSteps.length) {`,
`        setLoadingStep(step);`,
`        timer = setTimeout(advance, 8000);`,
`      }`,
`    }`,
`    let timer = setTimeout(advance, 8000);`,
`    return () => clearTimeout(timer);`,
`  }, [loading]);`,
``,
`  async function fetchUrl() {`,
`    if (!url.trim()) return;`,
`    try {`,
`      setFetching(true);`,
`      setFetchMessage("");`,
`      setError("");`,
`      const response = await fetch("/api/scrape", {`,
`        method: "POST",`,
`        headers: { "Content-Type": "application/json" },`,
`        body: JSON.stringify({ url }),`,
`      });`,
`      const data = await response.json();`,
`      if (data.error) { setError(data.error); }`,
`      else { setInput(data.content); setFetchMessage("Page fetched successfully!"); setActiveTab("paste"); }`,
`    } catch (err) { setError("Failed to fetch. Please paste content manually."); }`,
`    finally { setFetching(false); }`,
`  }`,
``,
`  async function analyze() {`,
`    if (!input.trim()) return;`,
`    try {`,
`      setLoading(true); setError(""); setAnalysis("");`,
`      const response = await fetch("/api/analyze", {`,
`        method: "POST",`,
`        headers: { "Content-Type": "application/json" },`,
`        body: JSON.stringify({ input, language: "Auto-Detect", mode }),`,
`      });`,
`      const data = await response.json();`,
`      if (data.error) { setError(data.error); }`,
`      else { setAnalysis(data.result); }`,
`    } catch (err) { setError("Something went wrong. Please try again."); }`,
`    finally { setLoading(false); }`,
`  }`,
``,
`  async function submitEmail() {`,
`    if (!email.trim() || !email.includes("@")) return;`,
`    try {`,
`      setEmailLoading(true);`,
`      const response = await fetch("/api/capture-email", {`,
`        method: "POST",`,
`        headers: { "Content-Type": "application/json" },`,
`        body: JSON.stringify({ email, language: "Auto-Detect" }),`,
`      });`,
`      const data = await response.json();`,
`      if (data.success) { setEmailSubmitted(true); downloadPDF(); }`,
`    } catch (err) { downloadPDF(); }`,
`    finally { setEmailLoading(false); }`,
`  }`,
``,
`  function downloadPDF() {`,
`    const doc = new jsPDF();`,
`    const pageWidth = doc.internal.pageSize.getWidth();`,
`    const margin = 15;`,
`    const maxWidth = pageWidth - margin * 2;`,
`    let y = 20;`,
`    doc.setFillColor(6, 182, 212);`,
`    doc.rect(0, 0, pageWidth, 28, "F");`,
`    doc.setTextColor(255, 255, 255);`,
`    doc.setFontSize(20);`,
`    doc.setFont("helvetica", "bold");`,
`    doc.text("JARVIS Marketing Report", margin, 18);`,
`    y = 40;`,
`    doc.setTextColor(30, 30, 30);`,
`    const lines = analysis.split("\\n");`,
`    for (const line of lines) {`,
`      if (y > 270) { doc.addPage(); y = 20; }`,
`      if (line.startsWith("# ")) {`,
`        y += 4; doc.setFontSize(14); doc.setFont("helvetica", "bold"); doc.setTextColor(6, 182, 212);`,
`        doc.text(line.replace("# ", ""), margin, y); y += 2;`,
`        doc.setDrawColor(6, 182, 212); doc.setLineWidth(0.5); doc.line(margin, y, pageWidth - margin, y); y += 6;`,
`        doc.setTextColor(30, 30, 30);`,
`      } else if (line.startsWith("## ")) {`,
`        y += 3; doc.setFontSize(12); doc.setFont("helvetica", "bold"); doc.setTextColor(6, 182, 212);`,
`        doc.text(line.replace("## ", ""), margin, y); y += 6; doc.setTextColor(30, 30, 30);`,
`      } else if (line.startsWith("> ")) {`,
`        doc.setFontSize(10); doc.setFont("helvetica", "bolditalic");`,
`        const clean = line.replace(/^> /, "").replace(/\\*\\*/g, "");`,
`        const wrapped = doc.splitTextToSize(clean, maxWidth - 10);`,
`        for (const wline of wrapped) { if (y > 270) { doc.addPage(); y = 20; } doc.text(wline, margin + 5, y); y += 5; }`,
`      } else if (line.trim() === "" || line.trim() === "---") {`,
`        y += 3;`,
`      } else {`,
`        doc.setFontSize(10); doc.setFont("helvetica", "normal"); doc.setTextColor(30, 30, 30);`,
`        const clean = line.replace(/\\*\\*/g, "").replace(/\\*/g, "");`,
`        const wrapped = doc.splitTextToSize(clean, maxWidth);`,
`        for (const wline of wrapped) { if (y > 270) { doc.addPage(); y = 20; } doc.text(wline, margin, y); y += 5; }`,
`      }`,
`    }`,
`    doc.setFontSize(8); doc.setTextColor(150, 150, 150);`,
`    doc.text("Generated by JARVIS Marketing Intelligence", margin, 290);`,
`    doc.save("jarvis-report.pdf");`,
`  }`,
``,
`  const { verdict, report } = splitVerdictAndReport(analysis);`,
``,
`  return (`,
`    <main style={{background: "#080b12", minHeight: "100vh"}}>`,
`      <nav style={{borderBottom: "1px solid #1e293b", padding: "16px 32px", display: "flex", justifyContent: "space-between", alignItems: "center"}}>`,
`        <Link href="/" style={{fontSize: "22px", fontWeight: "800", color: "#22d3ee", textDecoration: "none"}}>JARVIS</Link>`,
`        <Link href="/" style={{fontSize: "13px", color: "#64748b", textDecoration: "none"}}>Back to Home</Link>`,
`      </nav>`,
``,
`      <div style={{maxWidth: "800px", margin: "0 auto", padding: "40px 20px"}}>`,
``,
`        <div style={{marginBottom: "32px"}}>`,
`          <div style={{display: "inline-block", border: "1px solid rgba(34,211,238,0.4)", color: "#22d3ee", fontSize: "11px", fontWeight: "700", padding: "4px 14px", borderRadius: "20px", marginBottom: "16px", letterSpacing: "2px"}}>`,
`            MARKETING INTELLIGENCE`,
`          </div>`,
`          <h1 style={{fontSize: "32px", fontWeight: "800", color: "#ffffff", marginBottom: "12px", lineHeight: "1.2"}}>`,
`            Analyze Your Campaign`,
`          </h1>`,
`          <p style={{color: "#94a3b8", fontSize: "16px", lineHeight: "1.6"}}>`,
`            Paste your campaign data or URL. 4 specialist analyses run in parallel and tell you exactly what to fix.`,
`          </p>`,
`        </div>`,
``,
`        <div style={{display: "grid", gridTemplateColumns: "1fr 1fr", gap: "12px", marginBottom: "24px"}}>`,
`          <button onClick={() => setMode("quick")} style={{padding: "16px", borderRadius: "12px", border: mode === "quick" ? "2px solid #22d3ee" : "2px solid #1e293b", background: mode === "quick" ? "rgba(34,211,238,0.08)" : "#0f172a", cursor: "pointer", textAlign: "left", transition: "all 0.2s"}}>`,
`            <div style={{fontWeight: "700", fontSize: "15px", color: mode === "quick" ? "#22d3ee" : "#e2e8f0", marginBottom: "4px"}}>Quick Report</div>`,
`            <div style={{fontSize: "12px", color: "#64748b"}}>500 words - 15 seconds</div>`,
`          </button>`,
`          <button onClick={() => setMode("full")} style={{padding: "16px", borderRadius: "12px", border: mode === "full" ? "2px solid #22d3ee" : "2px solid #1e293b", background: mode === "full" ? "rgba(34,211,238,0.08)" : "#0f172a", cursor: "pointer", textAlign: "left", transition: "all 0.2s"}}>`,
`            <div style={{fontWeight: "700", fontSize: "15px", color: mode === "full" ? "#22d3ee" : "#e2e8f0", marginBottom: "4px"}}>Full Report</div>`,
`            <div style={{fontSize: "12px", color: "#64748b"}}>900 words - 40 seconds</div>`,
`          </button>`,
`        </div>`,
``,
`        <div style={{background: "#0f172a", border: "1px solid #1e293b", borderRadius: "16px", overflow: "hidden", marginBottom: "24px"}}>`,
`          <div style={{display: "flex", borderBottom: "1px solid #1e293b"}}>`,
`            <button onClick={() => setActiveTab("paste")} style={{flex: 1, padding: "14px", fontSize: "14px", fontWeight: "600", cursor: "pointer", border: "none", background: "transparent", color: activeTab === "paste" ? "#22d3ee" : "#64748b", borderBottom: activeTab === "paste" ? "2px solid #22d3ee" : "2px solid transparent"}}>`,
`              Paste Content`,
`            </button>`,
`            <button onClick={() => setActiveTab("url")} style={{flex: 1, padding: "14px", fontSize: "14px", fontWeight: "600", cursor: "pointer", border: "none", background: "transparent", color: activeTab === "url" ? "#22d3ee" : "#64748b", borderBottom: activeTab === "url" ? "2px solid #22d3ee" : "2px solid transparent"}}>`,
`              Paste URL`,
`            </button>`,
`          </div>`,
``,
`          <div style={{padding: "20px"}}>`,
`            {activeTab === "paste" ? (`,
`              <div>`,
`                {fetchMessage && <div style={{marginBottom: "12px", padding: "10px 14px", background: "rgba(34,197,94,0.1)", border: "1px solid rgba(34,197,94,0.3)", borderRadius: "8px", color: "#4ade80", fontSize: "13px"}}>{fetchMessage}</div>}`,
`                <textarea value={input} onChange={(e) => setInput(e.target.value)} style={{width: "100%", height: "180px", background: "#1e293b", border: "1px solid #334155", borderRadius: "10px", padding: "14px", color: "#e2e8f0", fontSize: "14px", resize: "none", outline: "none", fontFamily: "inherit", boxSizing: "border-box"}} placeholder="Paste your landing page, ad copy, offer, sales page, or campaign data..." />`,
`              </div>`,
`            ) : (`,
`              <div>`,
`                <div style={{display: "flex", gap: "10px"}}>`,
`                  <input type="url" value={url} onChange={(e) => setUrl(e.target.value)} style={{flex: 1, background: "#1e293b", border: "1px solid #334155", borderRadius: "10px", padding: "12px 14px", color: "#e2e8f0", fontSize: "14px", outline: "none", fontFamily: "inherit"}} placeholder="https://yourlandingpage.com" />`,
`                  <button onClick={fetchUrl} disabled={fetching || !url.trim()} style={{padding: "12px 20px", background: "#0891b2", color: "white", border: "none", borderRadius: "10px", fontWeight: "600", fontSize: "14px", cursor: "pointer", whiteSpace: "nowrap", opacity: fetching || !url.trim() ? 0.5 : 1}}>`,
`                    {fetching ? "Fetching..." : "Fetch Page"}`,
`                  </button>`,
`                </div>`,
`                <p style={{color: "#475569", fontSize: "12px", marginTop: "8px"}}>Paste a landing page URL and JARVIS will fetch the content automatically.</p>`,
`              </div>`,
`            )}`,
`          </div>`,
``,
`          <div style={{padding: "14px 20px", background: "#0a0f1a", borderTop: "1px solid #1e293b", display: "flex", alignItems: "center", justifyContent: "space-between", flexWrap: "wrap", gap: "12px"}}>`,
`            <div style={{display: "flex", alignItems: "center", gap: "8px"}}>`,
`              <div style={{width: "8px", height: "8px", borderRadius: "50%", background: "#22d3ee"}}></div>`,
`              <span style={{color: "#94a3b8", fontSize: "13px"}}>Auto-Detect Language</span>`,
`              <span style={{color: "#334155", fontSize: "13px"}}>Works in any language</span>`,
`            </div>`,
`            <button onClick={analyze} disabled={loading || !input.trim()} style={{padding: "12px 28px", background: loading || !input.trim() ? "#1e293b" : "#0891b2", color: loading || !input.trim() ? "#475569" : "white", border: "none", borderRadius: "10px", fontWeight: "700", fontSize: "14px", cursor: loading || !input.trim() ? "not-allowed" : "pointer", transition: "all 0.2s"}}>`,
`              {loading ? "Analyzing..." : "Analyze My Campaign"}`,
`            </button>`,
`          </div>`,
`        </div>`,
``,
`        {loading && (`,
`          <div style={{background: "#0f172a", border: "1px solid #1e293b", borderRadius: "16px", padding: "24px", marginBottom: "24px"}}>`,
`            <div style={{display: "flex", alignItems: "center", gap: "10px", marginBottom: "20px"}}>`,
`              <div style={{width: "8px", height: "8px", borderRadius: "50%", background: "#22d3ee", animation: "pulse 1s infinite"}}></div>`,
`              <span style={{color: "#22d3ee", fontWeight: "600", fontSize: "14px"}}>JARVIS Intelligence Engine Running</span>`,
`            </div>`,
`            <div style={{display: "flex", flexDirection: "column", gap: "12px"}}>`,
`              {loadingSteps.map((step, index) => (`,
`                <div key={index} style={{display: "flex", alignItems: "center", gap: "12px"}}>`,
`                  <div style={{width: "24px", height: "24px", borderRadius: "50%", background: index < loadingStep ? "#0891b2" : index === loadingStep ? "#0891b2" : "#1e293b", display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0}}>`,
`                    {index < loadingStep ? (`,
`                      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="white" strokeWidth="3"><path d="M5 13l4 4L19 7"/></svg>`,
`                    ) : (`,
`                      <div style={{width: "8px", height: "8px", borderRadius: "50%", background: index === loadingStep ? "white" : "#334155"}}></div>`,
`                    )}`,
`                  </div>`,
`                  <span style={{fontSize: "14px", color: index < loadingStep ? "#475569" : index === loadingStep ? "#ffffff" : "#475569", textDecoration: index < loadingStep ? "line-through" : "none"}}>`,
`                    {step}`,
`                  </span>`,
`                </div>`,
`              ))}`,
`            </div>`,
`          </div>`,
`        )}`,
``,
`        {error && (`,
`          <div style={{marginBottom: "24px", padding: "14px 16px", background: "rgba(239,68,68,0.1)", border: "1px solid rgba(239,68,68,0.3)", borderRadius: "10px", color: "#f87171", fontSize: "14px"}}>{error}</div>`,
`        )}`,
``,
`        {analysis && (`,
`          <div style={{background: "#0f172a", border: "1px solid #1e293b", borderRadius: "16px", overflow: "hidden"}}>`,
`            <div style={{background: "linear-gradient(135deg, #0e7490, #0891b2)", padding: "24px 28px"}}>`,
`              <div style={{display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px"}}>`,
`                <div style={{width: "6px", height: "6px", borderRadius: "50%", background: "rgba(255,255,255,0.7)"}}></div>`,
`                <span style={{color: "rgba(255,255,255,0.7)", fontSize: "11px", fontWeight: "700", letterSpacing: "2px"}}>INTELLIGENCE REPORT</span>`,
`              </div>`,
`              <h2 style={{fontSize: "24px", fontWeight: "800", color: "white", margin: 0}}>JARVIS Marketing Report</h2>`,
`              <p style={{color: "rgba(255,255,255,0.7)", fontSize: "14px", marginTop: "6px"}}>Find The Real Reason Your Campaign Is Not Converting</p>`,
`            </div>`,
``,
`            {verdict && (`,
`              <div style={{background: "#060911", padding: "24px 28px", borderBottom: "1px solid #1e293b"}}>`,
`                <div className="prose prose-invert max-w-none prose-h2:text-cyan-400 prose-h2:text-base prose-h2:font-bold prose-h2:mb-3 prose-h2:mt-0 prose-blockquote:border-l-4 prose-blockquote:border-cyan-500 prose-blockquote:pl-4 prose-blockquote:not-italic prose-blockquote:text-slate-300 prose-strong:text-white prose-p:text-slate-300">`,
`                  <ReactMarkdown>{verdict}</ReactMarkdown>`,
`                </div>`,
`              </div>`,
`            )}`,
``,
`            <div style={{padding: "28px"}}>`,
`              <div className="prose prose-invert max-w-none prose-sm sm:prose-base prose-h1:text-xl prose-h1:font-bold prose-h1:text-cyan-400 prose-h1:border-b prose-h1:border-slate-700 prose-h1:pb-2 prose-h1:mt-8 prose-h2:text-lg prose-h2:font-bold prose-h2:text-slate-200 prose-h2:mt-6 prose-strong:text-white prose-p:text-slate-300 prose-p:leading-relaxed prose-li:text-slate-300 prose-hr:border-slate-700">`,
`                <ReactMarkdown>{report}</ReactMarkdown>`,
`              </div>`,
``,
`              <div style={{marginTop: "32px", paddingTop: "24px", borderTop: "1px solid #1e293b"}}>`,
`                {!emailSubmitted ? (`,
`                  <div style={{background: "#1e293b", border: "1px solid #334155", borderRadius: "12px", padding: "20px"}}>`,
`                    <h3 style={{color: "white", fontWeight: "700", fontSize: "15px", marginBottom: "6px"}}>Download Your Full PDF Report</h3>`,
`                    <p style={{color: "#94a3b8", fontSize: "13px", marginBottom: "16px"}}>Enter your email to download and get notified of new JARVIS features.</p>`,
`                    <div style={{display: "flex", gap: "10px", flexWrap: "wrap"}}>`,
`                      <input type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="your@email.com" style={{flex: 1, minWidth: "200px", background: "#0f172a", border: "1px solid #334155", borderRadius: "8px", padding: "10px 14px", color: "#e2e8f0", fontSize: "14px", outline: "none", fontFamily: "inherit"}} />`,
`                      <button onClick={submitEmail} disabled={emailLoading || !email.trim()} style={{padding: "10px 20px", background: "#0891b2", color: "white", border: "none", borderRadius: "8px", fontWeight: "600", fontSize: "14px", cursor: "pointer", whiteSpace: "nowrap", opacity: emailLoading || !email.trim() ? 0.5 : 1}}>`,
`                        {emailLoading ? "Saving..." : "Download PDF"}`,
`                      </button>`,
`                    </div>`,
`                    <p style={{color: "#475569", fontSize: "12px", marginTop: "8px"}}>No spam. Unsubscribe anytime.</p>`,
`                  </div>`,
`                ) : (`,
`                  <div style={{display: "flex", alignItems: "center", justifyContent: "space-between", background: "#1e293b", borderRadius: "12px", padding: "16px 20px"}}>`,
`                    <span style={{color: "#4ade80", fontSize: "14px", fontWeight: "600"}}>Email saved! Your PDF is downloading.</span>`,
`                    <button onClick={downloadPDF} style={{padding: "8px 16px", background: "#0891b2", color: "white", border: "none", borderRadius: "8px", fontWeight: "600", fontSize: "13px", cursor: "pointer"}}>Download Again</button>`,
`                  </div>`,
`                )}`,
`              </div>`,
`            </div>`,
`          </div>`,
`        )}`,
`      </div>`,
`    </main>`,
`  );`,
`}`,
].join("\n");
writeFileSync("src/app/analyze/page.tsx", content, "utf8");
console.log("Done");
